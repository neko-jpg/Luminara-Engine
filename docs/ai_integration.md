# AI Integration Guide

## MCP Server

Run `luminara_mcp_server` to start the Model Context Protocol server. Connect your AI assistant (e.g. Claude) to it.

Tools available:
- `scene.create_entity`
- `script.create`
- `project.scaffold`

## CLI

Use `luminara ai generate --prompt "Make a platformer"` to scaffold a project.
